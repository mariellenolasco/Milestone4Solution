# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'
  
variables:
  buildConfiguration: 'Release'

steps:


- script: dotnet build ./Milestone3 --configuration $(buildConfiguration)
  displayName: 'dotnet build $(buildConfiguration)'

- task: DotNetCoreCLI@2
  displayName: dotnet test
  inputs:
    command: 'test'
    arguments: ./Milestone3 --configuration $(buildConfiguration) --collect "Code Coverage"


- task: AzureRmWebAppDeployment@4
  inputs:
    ConnectionType: 'AzureRM'
    azureSubscription: 'Azure subscription 1(cb0d2e9c-c632-4eec-93d0-d1e5f9589605)'
    appType: 'webApp'
    WebAppName: 'LodgingAPI'
    packageForLinux: '$(System.DefaultWorkingDirectory)/**/*.zip'